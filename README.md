# This project involves deploying a secure EC2 instances with a shared RDS database(MYSQL) on AWS using terraform. I configured a Virtual Private Cloud (VPC), spin up EC2 instances, set up an RDS database, and distribute incoming traffic with an Application Load Balancer (ALB).
# Finally I created a pipeline using githyb actions to automated deployment.
# My focus was on adhering to best practices for security, scalability, and maintainability.
